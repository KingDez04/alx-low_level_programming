#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: string to be checked
 *
 * Return: string_length
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i]; ++i)
	{
		;
	}

	return (i);
}
